# Git Save/Load 插件架构文档

## 概述

Git Save/Load 是一个 HanaAgent 平台 Card 插件，提供 Git 版本管理的核心功能。插件采用**事件总线 + 卡片组件**架构，每个功能区域为独立卡片，通过事件驱动通信。

- 插件 ID: `git-save-load`
- 版本: 2.2.0
- 平台: HanaAgent (OpenHanako)
- 开发语言: JavaScript (前端) + Node.js (后端)

---

## 文件结构

```
Git_Save-Load/
├── manifest.json            # 插件清单（ID、权限、配置项）
├── DESIGN.md                # Linear 风格设计规范
├── linear.DESIGN.md         # Linear 原版设计参考
├── routes/
│   ├── git.js               # 页面入口、公共 Git 辅助和路由装配
│   ├── local-git.js         # 状态、提交、身份和回滚
│   ├── history.js           # 提交历史查询
│   ├── history-edit.js      # amend、tag、squash、reword
│   ├── diff-conflicts.js    # diff、版本对比和冲突处理
│   ├── repository.js        # 仓库路径、仓库信息、初始化和版本
│   ├── github.js            # GitHub CLI 管理
│   ├── remote-query.js      # 远程列表和远程角色
│   ├── remote-sync.js       # 远程状态、fetch、merge 和 remove
│   ├── remote-edit.js       # 远程名称和地址编辑
│   ├── remote-push.js       # pull、push 和远程覆盖
│   ├── branch.js             # 分支管理
│   ├── stash.js              # Stash 管理
│   ├── config.js             # 配置读写
│   └── misc.js               # 兼容接口
├── assets/
│   ├── git.html            # Card 入口（DOM 结构 + 模块加载顺序）
│   ├── git.css             # 全部样式（拆分自原内联 style）
│   └── git/                # 前端 JS 模块（26 个，按原执行顺序加载）
├── views/
│   └── git.html             # 历史遗留视图，当前入口不使用
├── tools/
│   ├── _helpers.js          # 工具辅助函数（resolvePath）
│   ├── git_commit.js        # Agent 工具：存档
│   ├── git_log.js           # Agent 工具：提交历史
│   ├── git_reset.js         # Agent 工具：回滚
│   └── git_status.js        # Agent 工具：状态

---

## 架构

### 事件总线（Event Bus）

插件内部通信的核心机制，实现卡片间的松耦合。

```
Bus.on("commit", fileCard.refresh)    // 文件卡片说：听到commit就刷新
Bus.on("commit", logCard.refresh)     // 日志卡片说：我也要刷新
Bus.emit("commit")                     // 存档按钮说：我提交了
```

**核心 API：**

```javascript
Bus.on(event, callback)     // 订阅事件
Bus.emit(event, data)       // 发射事件
Bus.off(event, callback)    // 取消订阅
```

**事件列表：**

| 事件 | 发射者 | 响应者 |
|------|--------|--------|
| `commit` | 存档按钮 | fileCard, logCard |
| `push` | 推送按钮 | fileCard |
| `pull` | 拉取按钮 | fileCard |
| `reset` | 回滚确认 | fileCard, logCard |
| `branch-switch` | 切换分支 | fileCard, logCard |
| `refresh-all` | 刷新按钮 | 所有已注册卡片 |

### 卡片注册系统

```javascript
Cards.register("fileCard", {
  events: ["commit", "push", "pull", "reset", "branch-switch", "refresh-all"],
  refresh: function() {
    var p = currentPath || getSavedPath();
    if (p) fetch("/api/status?...").then(renderStatus);
  }
});
```

已注册卡片：

| 卡片 ID | 订阅事件 | 功能 |
|---------|----------|------|
| fileCard | commit, push, pull, reset, branch-switch, refresh-all | 变更文件状态 |
| logCard | commit, reset, branch-switch, refresh-all | 提交历史列表 |
| stashCard | commit, stash-push, stash-pop, stash-drop, refresh-all | 暂存列表 |

### 分支画布（独立视图）

分支画布不注册为卡片，而是作为独立的全屏视图，有自己的坐标系、拖拽逻辑和 SVG 连线渲染。

---

## 功能列表

### 主界面卡片

| 卡片 | 功能 |
|------|------|
| 仓库路径 | 设置/切换 Git 仓库路径，历史记录 |
| 存档 | 输入提交消息 → add + commit + 可选 tag |
| 变更文件 | 显示文件变更状态（M/A/D/R/U/??），点击查看 diff |
| 提交记录 | 最近提交历史，点击 hash 回滚 |
| 暂存 | 临时存放修改，可恢复/删除 |
| 设置 | 暂存模式/推送模式/拉取模式/Diff 模式/主题/GitHub打开方式 |
| GitHub | 创建/克隆/列表/搜索远程仓库 |

### 分支画布

- 方块自由拖拽
- 画布平移（空白处拖拽）
- SVG 贝塞尔曲线连线（按命名前缀推断父子关系）
- 右键菜单：创建分支（普通/基于当前/基于 commit）、切换分支、设置颜色、备注
- 连线标注（双击编辑）
- 分支颜色标识（master=绿，feature=蓝，fix=橙 等）
- 自定义颜色（右键设置）
- 布局持久化（localStorage）

### 后端 API（49 个方法注册，47 个路径）

- 状态、存档、身份配置和回滚
- 提交历史查询与历史编辑
- Diff、版本对比和冲突解决
- 分支管理（创建、删除、切换）
- 远程查询、角色配置、fetch、merge、pull、push 和远程覆盖
- 远程名称与地址编辑
- Stash 管理
- GitHub 集成（创建、克隆、列表、搜索、删除、编辑和关联）
- 版本号管理
- 配置读写
- 外部浏览器打开

接口按职责拆分到多个 `routes/*.js` 模块，`routes/git.js` 只负责页面入口、公共辅助函数和模块装配。

---

## 实现细节

### 主题系统

使用 CSS 自定义属性实现深色/浅色主题切换：

```css
:root {
  --hana-bg: #f5f6f6;        /* Linear 浅色默认 */
  --hana-accent: #5e6ad2;    /* 蓝紫色主色 */
}
.theme-dark {
  --hana-bg: #010102;         /* Linear 深色 */
}
```

通过 `document.body.classList.toggle("theme-dark")` 切换。主题选择保存在 localStorage + 后端配置。

### 分支颜色

按命名约定自动分配颜色，支持用户自定义覆盖：

```javascript
function branchColor(name) {
  var custom = localStorage.getItem(":color:" + name);
  if (custom) return custom;                    // 自定义颜色优先
  if (name === "master") return "#27a644";      // 绿色
  if (name.startsWith("feature/")) return "#5e6ad2";  // 蓝色
  if (name.startsWith("fix/")) return "#e67e22";      // 橙色
  if (name.startsWith("hotfix/")) return "#e74c3c";   // 红色
  // ...
}
```

### 卡片拖拽排序

使用 HTML5 Drag and Drop API。拖拽时蓝色指示线显示插入位置，松开后保存顺序到 localStorage。

```javascript
container.addEventListener("dragover", function(e) {
  // 检测鼠标在卡片上半部/下半部 → before/after
  // 移动指示线到对应位置
});
container.addEventListener("drop", function(e) {
  // 将源卡片插入到指示线位置
  // 保存新顺序
});
```

### 缓存机制

状态和日志数据缓存 10 秒，写操作时自动清除缓存：

```javascript
function cacheSet(key, data) {
  localStorage.setItem("gsl-cache-" + key, JSON.stringify({ t: Date.now(), d: data }));
}
function cacheGet(key) {
  var raw = JSON.parse(localStorage.getItem("gsl-cache-" + key));
  if (raw && Date.now() - raw.t < 10000) return raw.d;
  return null;
}
```

### 安全措施

- 用户输入经 `escapeHtml()` 转义后入 DOM
- 文件名等数据使用 `data-*` 属性 + 事件委托，避免内联 onclick 注入
- GitHub CLI 和 Git 命令均使用参数数组执行
- 远程名称、分支名称和 URL 在后端进行格式校验
- 强推、远程删除、远程编辑和历史重写保留二次确认
- `windowsHide: true` 避免终端闪现

### 当前入口与遗留文件

当前 Manifest 声明的 Card 路由为 `/git`，后端从 `assets/git.html` 读取页面内容。页面只保留 DOM 结构和模块加载顺序：样式在 `assets/git.css`，前端逻辑在 `assets/git/*.js`（26 个模块，按拆分前的顶层执行顺序加载）。

桌面本地模式的卡片 iframe 仅携带 surface session 查询凭证，宿主既不为它派发资产 cookie，且 `/assets/*` 的授权策略要求 chat scope（surface 凭证无 scope），静态子资源会 403。因此页面返回时，后端把 `assets/...` 引用重写为插件自身路由 `git-asset/...`（`plugin_route` 策略允许匹配插件的 surface 凭证，与卡片内 API 请求同一权限模型），并追加基于文件 mtime+size 的 `?v=` 版本参数与同名 query 会话回传；版本参数保证文件更新后 WebView 立即拿到新内容，`git.html` 本身按文件签名自动重读。

`views/git.html` 和 `assets/git-api.js` 在插件源码中没有运行时引用，目前作为历史遗留文件保留，后续可在独立清理提交中删除。
